import { defineConfig } from 'astro/config'

export default defineConfig({
  site: 'https://alvinmlna.github.io',
  base: '/portofolio',
  trailingSlash: 'always', // Use to always append '/' at end of url
})